# page no: 503

H = 20;
Bt = 6;
s1 = 4;
s2 = 3;
fb = 2;
gamma_d = 17.27;
wc = 0.15;
gamma_s = 21.19;
gamma_w = 9.81;
wavg = 19.62;
theta = 26;
co = 19.13;
K = 5e-4;
gamma_f = 17.27;
cof = 47.87;
fi = 8;
t = 6;
FOSp = 1.5;
PS = 8e-6;
An = 302.4;
At = 91.2;
Au = 98.4;
Le = 60;
SumN = An*gamma_s;
SumT = At*gamma_s;
SumU = Au*gamma_w;
F = ((Le*co)+(SumN-SumU)*tan(deg2rad(theta)))/SumT;
print(F);
h1 = 15.;
b = 80;
P = gamma_s*H^2*tand(45-(theta/2))^2/2+gamma_w*h1^2/2;
sav = P/b;
smax = 2*sav;
Ne = (gamma_s-gamma_w)*b*H/2;
R = Ne*tand(theta)+co*b;
fs = R/P;
print(fs);
sr = 0.6*H*(gamma_s-gamma_w)*tand(theta)+co;
FS = sr/smax;
print(FS);

h1 = 26.;
h2 = 6.;
gamma_m = (wavg*(h1-h2)+gamma_f*h2)/h1;
l = (gamma_m*h1*tand(fi)+cof)/(gamma_m*h1);
fi1 = atand(l);
P = (h1^2-h2^2)/2*gamma_m*tand(45-(fi1/2))^2;
sav = P/b;
smax = 2*sav;
s1 = cof+gamma_f*h2*tand(fi);
s2 = cof+gamma_m*h1*tand(fi);
as = (s1+s2)/2;
fs = as/sav;
fs = round(fs*100)/100;
print(fs);
gamma_av = (wavg*0.6*H+gamma_f*h2)/((0.6*H)+h2);
s = cof+gamma_av*0.6*H*tand(fi);
fs = s/smax;
print(fs);
s = 2;
q = K*s*100000/100;
print(q);
